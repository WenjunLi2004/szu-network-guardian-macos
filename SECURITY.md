# Security policy

## 凭据

- 账号和密码只保存于当前用户的 macOS Keychain。
- Keychain helper 的 `set` 操作只从标准输入读取秘密。
- LaunchAgent、配置文件和日志不得包含账号或密码。
- 菜单栏凭据输入框保存后立即清空。

请勿在 Issue、Pull Request 或日志附件中提交真实账号、密码、challenge、认证载荷、
完整认证 URL 或未脱敏配置。

## 网络请求

- `requests.Session.trust_env = False`，避免系统代理造成联网状态误判。
- 教学区 SRun 和两个联网检测请求均启用 TLS 证书验证。
- 宿舍 HTTP 请求禁止自动重定向，防止带凭据查询参数被转发到其他地址。
- 日志只记录程序内部定义的固定事件，不记录 HTTP 异常文本或服务器响应正文。
- 程序不启动网络监听端口。

## 宿舍接口

`http://172.30.255.42:801/eportal/portal/login/` 是学校提供的明文 HTTP GET 接口。
使用宿舍模式时，密码仍会出现在校园网链路上的请求 URL 中。客户端无法通过日志脱敏、
禁止重定向或本地 Keychain 消除这项传输风险。

## 漏洞报告

公开报告请只提供最小复现和脱敏信息。涉及真实凭据或可利用细节时，应通过仓库维护者
提供的私密安全报告渠道联系，不要创建公开 Issue。
